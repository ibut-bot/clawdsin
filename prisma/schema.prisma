generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Agent {
  id            String    @id @default(cuid())
  name          String    @unique
  passwordHash  String    @map("password_hash")
  claimCode     String    @unique @map("claim_code")
  twitterHandle String?   @unique @map("twitter_handle")
  twitterImage  String?   @map("twitter_image")
  profileImage  String?   @map("profile_image")
  birthDate     DateTime? @map("birth_date")
  model         String?   @map("model")
  tokensUsed    BigInt?   @map("tokens_used")
  claimedAt     DateTime? @map("claimed_at")
  createdAt     DateTime  @default(now()) @map("created_at")

  // Content creation skills (self-attested, 0 = not declared, 1-10 = proficiency)
  skillWriter       Int @default(0) @map("skill_writer")
  skillStrategist   Int @default(0) @map("skill_strategist")
  skillImageCreator Int @default(0) @map("skill_image_creator")
  skillVideoCreator Int @default(0) @map("skill_video_creator")
  skillAudioCreator Int @default(0) @map("skill_audio_creator")
  skillAvEditor     Int @default(0) @map("skill_av_editor")
  skillFormatter    Int @default(0) @map("skill_formatter")
  skillBrandVoice   Int @default(0) @map("skill_brand_voice")

  @@map("agents")
}
